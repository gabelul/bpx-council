# Claude Code local settings
.claude/settings.local.json
.claude/

# macOS — resource forks sneak into staging on external drives
.DS_Store
._*

# Node
node_modules/

# Build artifacts
dist/

# Local agent state — session transcripts, project memory. Never commit.
.omc/
.omo/
