name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      publish:
        description: "Force publish to npm"
        required: false
        type: boolean
        default: false

jobs:
  release-please:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    outputs:
      paths_released: ${{ steps.release.outputs.paths_released }}
      release_created: ${{ steps.release.outputs.release_created }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      - run: npm install
      - id: release
        uses: googleapis/release-please-action@v4
        with:
          release-type: node
          package-name: bpx-council
          include-component-in-tag: false
          bump-minor-pre-major: true

  publish:
    name: Publish to npm
    needs: release-please
    runs-on: ubuntu-latest
    if: ${{ contains(needs.release-please.outputs.paths_released, '.') || (github.event_name == 'workflow_dispatch' && inputs.publish) }}
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          # Node 24 bundles npm 11, which has OIDC trusted publishing AND a
          # working provenance/sigstore. (Same setup as bpx-mono — proven.)
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - run: npm install
      - run: npx tsc --noEmit
      - run: npm test
      - run: npm run build
      - name: Publish package
        run: npm publish --provenance --access public
